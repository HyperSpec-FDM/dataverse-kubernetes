apiVersion: batch/v1
kind: CronJob
metadata:
  name: dataverse-deleter
  namespace: dv-test
spec:
  schedule: "*/1 * * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: dataverse-curler-service-account
          automountServiceAccountToken: true
          containers:
          - name: dataverse-deleter
            image: 192.168.100.11:31000/dataverse_deleter:1.0
            imagePullPolicy: IfNotPresent
            env:
              - name: url
                value: "http://localhost:8080/"
              - name: api_key
                value: "21d306cf-2a50-4d43-8a87-7556bec11d18"
              - name: dataverse_id
                value: "HyperSpec-FDM"
              - name: deployment_name
                value: "dataverse"
              - name: namespace
                value: "dv-test"
              - name: container_name
                value: "dataverse"
          restartPolicy: OnFailure
          imagePullSecrets:
            - name: registry-auth