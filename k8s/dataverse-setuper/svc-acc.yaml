apiVersion: v1
kind: ServiceAccount
metadata:
  name: dataverse-setuper-service-account
  namespace: dv-test
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: dataverse-setuper-cluster-role
  namespace: dv-test
rules:
  - apiGroups:
        - ""
        - apps
        - autoscaling
        - batch
        - extensions
        - policy
        - rbac.authorization.k8s.io
        - networking.k8s.io
    resources:
      - pods
      - pods/exec
      - deployments
      - namespaces
      - nodes
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: dataverse-setuper-cluster-role-binding
subjects:
- namespace: dv-test
  kind: ServiceAccount
  name: dataverse-setuper-service-account
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: dataverse-setuper-cluster-role