---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql
spec:
  storageClassName: csi-nfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
  # storageClassName: pm-local


