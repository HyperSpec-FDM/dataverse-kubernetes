---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dataverse-files
spec:
  storageClassName: csi-nfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dataverse-docroot
spec:
  storageClassName: csi-nfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi


